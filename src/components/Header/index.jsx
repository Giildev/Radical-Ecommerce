import React from 'react';
import { PageHeader } from 'react-bootstrap';
import './style.css';

const Header = () => (
	<div>
		<PageHeader className="header">Example Header</PageHeader>
	</div>
);

export default Header;
